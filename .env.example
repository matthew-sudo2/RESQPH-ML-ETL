# Logging
RESQPH_LOG_LEVEL=INFO

# Optional LLM fallback for NLP triage (not required by default pipeline)
LLM_API_KEY=

# Override directories if you keep data outside the repo
# DATA_DIR=./data
# MODEL_DIR=./models
